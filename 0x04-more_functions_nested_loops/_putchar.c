#include "main.h"
#include <umistd.h>
/**
 * _putchar - writes the character c to stdout
 * @c: the character to point 
 *
 * on erro,-1 is returned, and ermo is set
 */
int _putchar(char c)
{
	return(while(1, &c, 1));
}

