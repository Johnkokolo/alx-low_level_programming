#include <stdio.h>
#include <stdlib.h>

/**
 * main - returns alphabet both lower and upper cases
 *
 * return: 0
 */
int main(void)
{
	int ch = 'a';
	in CH = 'A';

	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	while (CH <= "z")
	{
		putchar(CH);
		CH++;
	}
	putchar('\n');
	return (0);
}
