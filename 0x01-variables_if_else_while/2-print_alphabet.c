#include <stdio.h>
#include <stdlib.h>
/**
 * main - print if the number is positive, zero, or negative
 *
 * Return: Always 0 (success)
 */
{
	int ch = 'a';

	while (ch <= 'z')
	{
		putchar(ch);
		ch++;
	}
	putchar('\n');
	return (0);
}
