#include "main.h"

/**
 * _abs - computes the absolue value
 * @t: The integer to becomputed
 * Return: the absolute
 */
int _abs(int t)
{
	if (t >= 0)
	{
		return (t);
	}
	else
	{
		return (-t);
	}
}
