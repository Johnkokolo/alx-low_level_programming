#include <main.h>

/**
 * _isalpha - check the code 
 * @c: code to be checked 
 *
 * Return: 1 if character is a letter,0 otherwise
 */
int _isalpha (int c)
{
	if (( >= 'a' && c <= 'z') || (c >= 'A' && c <= 'z'))
	{
		return (1);
		{
			else
			{
				return (0);
			}
		}
	}
}
